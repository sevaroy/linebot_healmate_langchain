# 面試技術簡報 - GraphRAG 流程與 AI 代理核心程式碼

## 🎯 簡報目標

展示對 **Microsoft GraphRAG 技術原理** 和 **LangChain AI 代理運作機制** 的深度理解，通過 5 個核心程式碼片段證明技術掌握能力和實戰經驗。

---

## 📋 程式碼片段總覽

| 順序 | 程式碼片段 | 技術重點 | 展示時間 |
|------|------------|----------|----------|
| 1 | GraphRAG 知識圖譜構建流程 | GraphRAG 多階段處理原理 | 5 分鐘 |
| 2 | Local vs Global Search 實現 | 雙重檢索策略技術差異 | 4 分鐘 |
| 3 | LangChain Agent 決策執行 | AI 代理運作機制 | 4 分鐘 |
| 4 | 智能工具選擇與執行邏輯 | Tool 系統設計原理 | 3 分鐘 |
| 5 | GraphRAG 向量化策略 | 多層級嵌入技術創新 | 4 分鐘 |

**總時間：20 分鐘**

---

## 🔧 程式碼片段 1: GraphRAG 知識圖譜構建流程 ⭐⭐⭐⭐⭐

### 💡 面試價值
- 展現對 Microsoft GraphRAG 核心技術的深度理解
- 證明能掌握複雜 AI 系統的多階段處理流程
- 體現對知識圖譜構建原理的技術洞察

### 📝 程式碼展示

```python
async def index_knowledge_base(self, kb_id: str) -> Dict[str, Any]:
    """🔍 Microsoft GraphRAG 完整知識圖譜構建流程
    
    這是 GraphRAG 的核心技術實現，展現了從非結構化文本
    到結構化知識圖譜的完整轉換過程
    """
    try:
        kb_path = Path(kb_info['path'])
        
        # 🔐 安全的 API 密鑰管理
        env = os.environ.copy()
        env["GRAPHRAG_API_KEY"] = self.openai_api_key
        
        # 🚀 執行 Microsoft GraphRAG 完整處理流水線
        result = subprocess.run([
            "python", "-m", "graphrag", "index",
            "--root", str(kb_path),
            "--verbose"
        ], capture_output=True, text=True, cwd=str(kb_path), env=env)
        
        """
        📊 GraphRAG 內部處理的 6 個關鍵階段：
        
        Stage 1: 📝 Text Chunking (文本分塊)
        ├── 讀取 input/ 目錄中的所有文件
        ├── 將長文本分割成 300-1200 tokens 的文本單元
        └── 保持語義完整性，避免重要信息截斷
        
        Stage 2: 🎯 Entity Extraction (實體提取)
        ├── 使用 LLM (GPT-4) 從每個文本單元提取實體
        ├── 識別類型：人物、組織、地點、概念、事件
        ├── 生成詳細的實體描述和屬性
        └── 輸出：entities.parquet (實體資料表)
        
        Stage 3: 🔗 Relationship Extraction (關係提取)
        ├── 分析實體間的語義關係
        ├── 提取關係類型：包含、屬於、影響、相關等
        ├── 建立實體關係網絡
        └── 輸出：relationships.parquet (關係資料表)
        
        Stage 4: 🏘️ Community Detection (社群偵測)
        ├── 使用圖算法 (Leiden algorithm) 進行社群偵測
        ├── 將相關實體組織成主題社群
        ├── 建立多層級的階層結構
        └── 輸出：communities.parquet (社群資料表)
        
        Stage 5: 📊 Community Summarization (社群總結)
        ├── 為每個社群生成總結描述
        ├── 使用 LLM 分析社群的核心主題
        ├── 創建結構化的社群報告
        └── 輸出：community_reports.parquet (社群報告)
        
        Stage 6: 🔢 Multi-level Embedding (多層級向量嵌入)
        ├── 實體描述嵌入 → entity-description vectors
        ├── 社群內容嵌入 → community-full_content vectors  
        ├── 文本單元嵌入 → text_unit-text vectors
        └── 儲存到 LanceDB 向量資料庫
        """
        
        if result.returncode == 0:
            return {
                'success': True,
                'message': '知識圖譜構建完成',
                'technical_details': {
                    'entities_extracted': '自動提取實體和屬性',
                    'relationships_mapped': '建立實體關係網絡',
                    'communities_detected': '智能社群偵測完成',
                    'embeddings_generated': '多層級向量嵌入',
                    'graph_constructed': '完整知識圖譜構建'
                }
            }
        else:
            return {
                'success': False,
                'error': result.stderr,
                'troubleshooting': '檢查 API 密鑰和文件格式'
            }
            
    except Exception as e:
        logging.error(f"GraphRAG indexing failed: {e}")
        return {'success': False, 'error': str(e)}
```

### 🎤 面試講解要點

1. **技術深度展示**：
   - "這個函數展現了我對 Microsoft GraphRAG 完整流水線的深度理解"
   - "從非結構化文本到結構化知識圖譜的 6 階段轉換過程"

2. **技術創新理解**：
   - "相比傳統 RAG，GraphRAG 的創新在於建立了實體關係網絡"
   - "社群偵測算法讓我們能發現文本中的隱藏主題結構"

3. **實際應用經驗**：
   - "在實際開發中，我遇到了 CLI 整合和環境變數管理的挑戰"
   - "通過安全的 API 密鑰傳遞解決了敏感信息洩露問題"

---

## 🔍 程式碼片段 2: Local vs Global Search 檢索策略 ⭐⭐⭐⭐⭐

### 💡 面試價值
- 展現對 GraphRAG 雙重檢索策略的深度理解
- 證明能區分不同檢索模式的技術原理和適用場景
- 體現對 Map-Reduce 模式在知識檢索中的應用理解

### 📝 程式碼展示

```python
async def _query(self, kb_id: str, query: str, method: str) -> Dict[str, Any]:
    """🔍 GraphRAG 雙重檢索策略實現
    
    展現 Local Search 和 Global Search 兩種檢索模式的
    技術差異和適用場景選擇邏輯
    """
    try:
        # 📋 業務邏輯驗證
        if kb_info['status'] != 'ready':
            return {
                'success': False,
                'message': '知識庫尚未準備就緒，請先建立索引'
            }
        
        kb_path = Path(kb_info['path'])
        query_type = "local" if method == "local" else "global"
        
        # 🔐 安全的查詢執行
        env = os.environ.copy()
        env["GRAPHRAG_API_KEY"] = self.openai_api_key
        
        result = subprocess.run([
            "python", "-m", "graphrag", "query",
            "--root", str(kb_path),
            "--method", query_type,
            "-q", query
        ], capture_output=True, text=True, cwd=str(kb_path), env=env)
        
        """
        🎯 Local Search (本地搜尋) 技術原理：
        
        適用場景：具體的事實性問題
        ├── "誰是某某人？"
        ├── "什麼是機器學習？"  
        └── "某事件何時發生？"
        
        技術流程：
        1. 🔢 Query Embedding
           └── 將用戶查詢轉換為向量表示
           
        2. 🎯 Entity Vector Search  
           ├── 在實體嵌入空間中搜尋相似實體
           └── 使用餘弦相似度進行匹配
           
        3. 📝 Text Unit Retrieval
           ├── 檢索包含相關實體的文本單元
           └── 基於向量相似度排序
           
        4. 🔗 Relationship Expansion
           ├── 擴展相關實體的關係鏈
           └── 構建局部知識子圖
           
        5. 📊 Context Assembly
           ├── 組裝檢索到的上下文信息
           └── 確保信息的完整性和相關性
           
        6. 💬 LLM Generation
           └── 基於檢索上下文生成精確回答
        
        技術特點：
        ✅ 高精度：直接匹配相關實體和文本
        ✅ 可解釋：可以追溯到具體的文本來源
        ✅ 快速響應：向量檢索效率高
        """
        
        """
        🌐 Global Search (全域搜尋) 技術原理：
        
        適用場景：概念性、總結性問題
        ├── "整體趨勢是什麼？"
        ├── "主要影響因素有哪些？"
        └── "請概述相關主題"
        
        技術流程 (Map-Reduce 模式)：
        1. 🏘️ Community Selection
           ├── 基於查詢選擇相關的知識社群
           └── 使用社群向量進行匹配
           
        2. 🗺️ Map Phase (並行處理)
           ├── 同時分析多個社群報告
           ├── 每個社群生成局部回答
           └── 並行處理提升效率
           
        3. 🔄 Reduce Phase (整合合成)
           ├── 收集所有局部回答
           ├── 使用 LLM 進行摘要和合成
           ├── 消除重複和矛盾信息
           └── 發現跨社群的關聯模式
           
        4. 📋 Final Synthesis
           ├── 生成最終的綜合回答
           ├── 整合多個視角的洞察
           └── 提供全域性的分析結果
        
        技術特點：
        ✅ 全面性：整合多個社群的知識
        ✅ 洞察力：發現隱藏的主題和關聯  
        ✅ 系統性：提供結構化的分析視角
        """
        
        if result.returncode == 0:
            answer = result.stdout.strip()
            
            # 📊 技術解釋和性能指標
            technical_explanation = {
                "local": {
                    "approach": "向量相似度檢索 + 實體關係擴展",
                    "strength": "高精度的事實性回答",
                    "data_source": "實體向量 + 文本單元向量"
                },
                "global": {
                    "approach": "Map-Reduce 社群分析",
                    "strength": "全面的概念性洞察", 
                    "data_source": "社群報告 + 階層結構"
                }
            }
            
            return {
                'success': True,
                'answer': answer,
                'query_type': method,
                'technical_approach': technical_explanation[method],
                'kb_id': kb_id
            }
            
    except Exception as e:
        return {'success': False, 'error': str(e)}
```

### 🎤 面試講解要點

1. **檢索策略對比**：
   - "Local Search 使用向量相似度檢索，適合精確的事實性問題"
   - "Global Search 採用 Map-Reduce 模式，適合概念性分析"

2. **技術原理理解**：
   - "Local Search 的核心是實體關係擴展，確保回答的準確性"
   - "Global Search 透過社群分析發現跨主題的隱藏關聯"

3. **場景應用判斷**：
   - "根據問題類型智能選擇檢索策略，提升回答品質"

---

## 🤖 程式碼片段 3: LangChain Agent 決策執行流程 ⭐⭐⭐⭐⭐

### 💡 面試價值
- 展現對 LangChain Agent 核心運作機制的深度理解
- 證明能設計和實現複雜的 AI 代理系統
- 體現對多元件協作架構的系統設計能力

### 📝 程式碼展示

```python
class LangChainAgentExecution:
    """🤖 LangChain Agent 完整執行邏輯
    
    展現 AI 代理的核心架構：LLM + Tools + Memory + Executor
    以及完整的決策執行流程
    """
    
    def __init__(self):
        # 🧠 LLM：Agent 的決策大腦
        self.llm = ChatDeepSeek(
            model="deepseek-chat", 
            temperature=0.7,
            streaming=True
        )
        
        # 🛠️ Tools：Agent 可用的工具生態系統
        self.tools = [
            *MICROSOFT_GRAPHRAG_TOOLS,  # GraphRAG 工具組
            emotion_analysis_tool,       # 情感分析工具
            tarot_reading_tool,         # 塔羅占卜工具
            strategy_tool,              # 策略建議工具
        ]
        
        # 💾 Memory：對話上下文管理系統
        self.memory = ConversationBufferWindowMemory(
            memory_key="chat_history",
            return_messages=True,
            k=10  # 保留最近 10 輪對話
        )
        
        # 🎯 Agent：決策執行器
        self.agent = create_openai_tools_agent(
            llm=self.llm,
            tools=self.tools,
            prompt=self._create_intelligent_prompt()
        )
        
        # 🔄 Executor：Agent 執行環境
        self.executor = AgentExecutor(
            agent=self.agent,
            tools=self.tools,
            memory=self.memory,
            verbose=True,               # 展示決策過程
            handle_parsing_errors=True, # 優雅錯誤處理
            max_iterations=5            # 防止無限循環
        )
    
    async def execute_user_query(self, user_input: str, user_id: str) -> str:
        """🚀 Agent 完整執行流程
        
        這是 LangChain Agent 的核心執行邏輯，展現了從用戶輸入
        到最終回應的完整決策鏈和推理過程
        """
        
        try:
            # 🎯 Step 1: 準備執行上下文
            execution_context = {
                "input": user_input,
                "user_id": user_id,
                "chat_history": self.memory.chat_memory.messages,
                "available_tools": [tool.name for tool in self.tools]
            }
            
            """
            🧠 LangChain Agent 內部決策流程詳解：
            
            Phase A: 🔍 Intent Recognition (意圖識別)
            ├── Agent 分析用戶輸入的語義和意圖
            ├── 考慮對話歷史的上下文信息
            ├── 識別用戶的真實需求和期望
            └── 判斷需要哪類工具來滿足需求
            
            Phase B: 🛠️ Tool Selection (工具選擇)
            ├── 基於工具描述進行語義匹配
            ├── 考慮工具的適用場景和限制
            ├── 評估多個工具的匹配程度
            └── 選擇最佳工具或工具組合
            
            Phase C: 📋 Parameter Extraction (參數提取)
            ├── 從用戶輸入中提取工具所需參數
            ├── 處理參數格式轉換和驗證
            ├── 補充必要的上下文信息
            └── 確保參數完整性和正確性
            
            Phase D: ⚡ Tool Execution (工具執行)
            ├── 調用選中的工具並傳遞參數
            ├── 處理工具執行的異步操作
            ├── 監控執行狀態和性能
            └── 捕獲和處理執行異常
            
            Phase E: 🤔 Result Reasoning (結果推理)
            ├── 分析工具返回的結果和數據
            ├── 判斷結果是否滿足用戶需求
            ├── 決定是否需要調用更多工具
            └── 進行多步推理和邏輯鏈接
            
            Phase F: 📝 Response Synthesis (回應合成)
            ├── 將工具結果整合成自然語言回應
            ├── 保持一致的語調和個性化風格
            ├── 添加相關的解釋和建議
            └── 確保回應的完整性和有用性
            """
            
            # 🧠 執行 Agent 決策和推理
            result = await self.executor.ainvoke(execution_context)
            
            # 📊 Step 2: 結果處理和品質保證
            response = result.get("output", "抱歉，我無法理解您的請求。")
            
            # 💾 Step 3: 更新對話記憶
            self.memory.save_context(
                {"input": user_input},
                {"output": response}
            )
            
            # 📈 Step 4: 執行指標記錄
            execution_metrics = {
                "tools_used": result.get("intermediate_steps", []),
                "tokens_consumed": self._estimate_tokens(user_input, response),
                "execution_time": result.get("execution_time", 0),
                "success": True
            }
            
            return response
            
        except Exception as e:
            logging.error(f"Agent execution failed: {e}")
            
            # 🛡️ 優雅的錯誤恢復
            fallback_response = self._generate_fallback_response(user_input, str(e))
            return fallback_response
    
    def _create_intelligent_prompt(self) -> ChatPromptTemplate:
        """🎯 智能系統提示設計
        
        這是 Agent 行為的核心控制邏輯，通過精心設計的提示
        來引導 LLM 進行正確的工具選擇和推理
        """
        
        system_prompt = """你是 HealMate，一個溫暖、智慧的 AI 助理。

🧠 **核心決策邏輯：**
1. 🔍 Deep Analysis: 深度分析用戶的真實需求和意圖
2. 🛠️ Tool-First: 優先使用工具獲取準確信息，避免憑空猜測
3. 💭 Context-Aware: 充分利用對話歷史和用戶狀態
4. 🎯 Multi-Step: 必要時進行多步推理和工具組合

🛠️ **智能工具選擇策略：**
- 📚 知識圖譜查詢 → Microsoft GraphRAG 工具組
  ├── 用戶提到「知識圖譜」、「建立知識庫」
  ├── 需要基於文件內容的深度分析  
  └── 涉及實體關係或概念性問題
  
- 💝 情感支持需求 → 情感分析 + 策略建議工具
  ├── 用戶表達情緒困擾或心理壓力
  ├── 需要心理健康支持和建議
  └── 要求情感狀態分析和改善策略
  
- 🔮 人生指引尋求 → 塔羅占卜工具
  ├── 用戶面臨重要決定或人生選擇
  ├── 尋求靈性指引或內在洞察
  └── 需要象徵性的啟發和思考角度

🗣️ **回應品質原則：**
- 自然融合工具結果，避免生硬的技術引用
- 保持溫暖、理解和支持的語調
- 提供 actionable 的建議和後續步驟
- 展現同理心和個人化關懷
- 確保回應的準確性和有用性

請根據用戶輸入，展現你的智能決策能力，選擇最合適的工具並提供有溫度的專業回應。"""

        return ChatPromptTemplate.from_messages([
            ("system", system_prompt),
            MessagesPlaceholder("chat_history"),
            ("human", "{input}"),
            MessagesPlaceholder("agent_scratchpad")
        ])
```

### 🎤 面試講解要點

1. **Agent 架構理解**：
   - "LangChain Agent 的核心是 LLM + Tools + Memory + Executor 的協作架構"
   - "每個元件都有明確的職責分工和交互機制"

2. **決策流程掌握**：
   - "從意圖識別到工具執行的 6 階段決策鏈展現了 AI 代理的智能推理能力"
   - "多步推理和工具組合讓 Agent 能處理複雜的用戶需求"

3. **Prompt Engineering**：
   - "系統提示的設計直接影響 Agent 的行為表現和工具選擇準確性"

---

## 🎯 程式碼片段 4: 智能工具選擇與執行邏輯 ⭐⭐⭐⭐

### 💡 面試價值
- 展現對 LangChain Tool 系統的深度理解
- 證明能設計智能化的工具選擇和執行邏輯
- 體現對用戶體驗和錯誤處理的細致考量

### 📝 程式碼展示

```python
async def _query_graphrag_tool(query: str, user_id: str = "default_user") -> str:
    """🎯 智能 GraphRAG 查詢工具
    
    展現 LangChain Tool 的完整執行邏輯：
    狀態檢查 → 資源選擇 → 策略決策 → 查詢執行 → 結果處理
    """
    try:
        # 🔍 Phase 1: 智能狀態檢查和用戶引導
        knowledge_bases = microsoft_graphrag_service.get_knowledge_bases(user_id)
        
        # 🚫 無知識庫的智能引導
        if not knowledge_bases:
            return """❌ 你還沒有建立任何 Microsoft GraphRAG 知識庫。

🚀 **建議下一步操作：**
1. 說「建立知識圖譜」來開始使用專業的 GraphRAG 技術
2. 上傳相關文件進行知識圖譜分析
3. 體驗先進的實體關係提取和社群偵測功能

💡 **GraphRAG 技術優勢：**
- 🧠 智能實體和關係提取
- 🏘️ 自動社群偵測和主題分析
- 🔍 支援本地精確檢索和全域洞察查詢
- 📊 多層級向量嵌入技術"""
        
        # 📋 智能狀態分析和處理
        ready_kbs = [kb for kb in knowledge_bases if kb['status'] == 'ready']
        
        if not ready_kbs:
            # 🔄 分析當前知識庫狀態並提供精確指導
            status_analysis = self._analyze_kb_status(knowledge_bases)
            
            return f"""❌ 你的 GraphRAG 知識庫還沒有建立索引。

📊 **當前狀態分析：**
{status_analysis}

📋 **完整操作流程：**
1️⃣ 上傳文件到知識庫 (支援 TXT, PDF, DOC, MD 等格式)
2️⃣ 執行 GraphRAG 索引建立 (進行實體提取和關係分析)
3️⃣ 等待索引完成後開始智能查詢

⏱️ **預計處理時間：** 根據文件大小，通常需要 2-5 分鐘
💡 **技術特色：** GraphRAG 索引建立會進行深度的語義分析，比傳統檢索更精確！"""
        
        # 🎯 Phase 2: 智能知識庫選擇策略
        selected_kb = self._select_optimal_kb(ready_kbs, query)
        kb_id = selected_kb['kb_id']
        
        # 🧠 Phase 3: 查詢策略智能決策
        """
        GraphRAG 查詢策略決策算法：
        
        基於查詢內容的智能分析來選擇最適合的檢索模式
        """
        query_strategy = self._determine_query_strategy(query)
        
        # ⚡ Phase 4: 執行 GraphRAG 查詢
        if query_strategy == "global":
            # 🌐 全域搜尋：適合概念性、總結性問題
            result = await microsoft_graphrag_service.query_global(kb_id, query)
            search_explanation = "基於社群分析的 Map-Reduce 整合查詢"
        else:
            # 🎯 本地搜尋：適合具體、事實性問題  
            result = await microsoft_graphrag_service.query_local(kb_id, query)
            search_explanation = "基於向量相似度的精確實體檢索"
        
        # 📊 Phase 5: 智能結果處理和格式化
        if result['success']:
            return f"""🔍 **Microsoft GraphRAG 查詢結果**

📚 **知識庫：** 「{selected_kb['name']}」
🧠 **查詢方式：** {search_explanation}
🎯 **策略選擇：** {query_strategy.upper()} Search

**📋 智能分析結果：**
{result['answer']}

💡 **技術說明：** 
{self._get_technical_explanation(query_strategy)}

🔗 **知識來源：** 基於 {len(selected_kb.get('documents', []))} 個文件的知識圖譜分析"""
        else:
            # 🛡️ 智能錯誤處理和故障排除指導
            return self._generate_error_guidance(result, selected_kb)
            
    except Exception as e:
        logging.error(f"GraphRAG tool execution error: {e}")
        return f"""❌ **GraphRAG 查詢過程中發生錯誤**

🔧 **錯誤信息：** {str(e)}

🛠️ **故障排除建議：**
1. 檢查知識庫索引狀態是否正常
2. 確認 API 密鑰配置是否正確
3. 驗證查詢內容格式是否有效
4. 嘗試重新建立索引或聯繫技術支援

💡 **技術支援：** 這個錯誤已記錄，我們會持續優化系統穩定性。"""

def _determine_query_strategy(self, query: str) -> str:
    """🧠 查詢策略智能決策算法
    
    基於查詢內容的語義分析來選擇最適合的 GraphRAG 檢索策略
    """
    
    # 🎯 Local Search 指標關鍵詞
    local_indicators = [
        "什麼是", "誰是", "何時", "在哪裡", "如何",
        "定義", "解釋", "具體", "詳細", "確切"
    ]
    
    # 🌐 Global Search 指標關鍵詞  
    global_indicators = [
        "總結", "整體", "趨勢", "關係", "影響", "原因",
        "比較", "分析", "概述", "主要", "全面", "綜合"
    ]
    
    # 📊 語義模式匹配計分
    local_score = sum(2 if indicator in query else 0 for indicator in local_indicators)
    global_score = sum(2 if indicator in query else 0 for indicator in global_indicators)
    
    # 🔍 查詢長度和複雜度分析
    if len(query) > 20 and "？" in query:
        global_score += 1  # 複雜問題傾向使用全域搜尋
    
    if len(query.split()) <= 5:
        local_score += 1   # 簡短問題傾向使用本地搜尋
    
    # 🎯 最終決策邏輯
    if global_score > local_score:
        return "global"
    elif local_score > 0:
        return "local" 
    else:
        # 🤖 智能預設策略：複雜問題使用全域，簡單問題使用本地
        return "global" if len(query) > 15 else "local"

def _get_technical_explanation(self, strategy: str) -> str:
    """📚 提供技術策略的深度解釋"""
    explanations = {
        "local": """本地搜尋採用向量相似度檢索技術：
        • 將查詢轉換為嵌入向量
        • 在實體和文本向量空間中查找最相似內容
        • 結合實體關係圖進行上下文擴展
        • 適合精確的事實性問題和具體信息查詢""",
        
        "global": """全域搜尋採用 Map-Reduce 社群分析技術：
        • 基於社群偵測算法組織的知識結構
        • 並行分析多個主題社群的相關內容
        • 通過 LLM 整合和合成跨社群的洞察
        • 適合概念性分析和總結性問題"""
    }
    return explanations.get(strategy, "智能查詢策略")
```

### 🎤 面試講解要點

1. **智能狀態管理**：
   - "工具會智能檢查前置條件，並提供精確的用戶引導"
   - "不同狀態下給出不同的操作建議，提升用戶體驗"

2. **策略決策算法**：
   - "通過語義分析自動選擇 Local 或 Global 檢索策略"
   - "結合查詢長度、關鍵詞匹配等多維度進行智能決策"

3. **錯誤處理設計**：
   - "每個可能的失敗點都有對應的故障排除指導"
   - "錯誤訊息具有可操作性，幫助用戶快速解決問題"

---

## 🔢 程式碼片段 5: GraphRAG 多層級向量化策略 ⭐⭐⭐⭐

### 💡 面試價值
- 展現對 GraphRAG 核心技術創新的深度理解
- 證明能掌握複雜的向量化和嵌入技術
- 體現對傳統 RAG 技術改進方向的技術洞察

### 📝 程式碼展示

```python
class GraphRAGEmbeddingStrategy:
    """🔢 GraphRAG 多層級向量化策略
    
    展現 GraphRAG 相比傳統 RAG 的核心技術創新：
    三層級嵌入設計和多粒度語義表示
    """
    
    def explain_multilevel_embedding_architecture(self):
        """📊 GraphRAG 三層級嵌入架構詳解
        
        這是 GraphRAG 的核心技術創新，通過多層級向量化
        實現從具體文本到抽象概念的完整語義覆蓋
        """
        
        embedding_architecture = {
            # 🔤 第一層：Text Unit Embeddings (文本單元嵌入)
            "text_unit_level": {
                "purpose": "原始文本的語義保留和精確檢索",
                "technical_details": {
                    "granularity": "文本塊級別 (300-1200 tokens)",
                    "processing": "分塊後的原始文本直接向量化",
                    "model": "text-embedding-3-small 或 ada-002",
                    "dimension": "1536-dimensional vectors",
                    "storage": "lancedb/text_unit-text/"
                },
                "use_cases": [
                    "精確的文本檢索和引用",
                    "保留原始語義和上下文",
                    "Local Search 的基礎數據源",
                    "支援具體內容的快速定位"
                ],
                "advantages": [
                    "✅ 保持原始語義完整性",
                    "✅ 支援精確的內容檢索", 
                    "✅ 可追溯到具體文本來源",
                    "✅ 適合事實性問題回答"
                ]
            },
            
            # 🎯 第二層：Entity Description Embeddings (實體描述嵌入)
            "entity_level": {
                "purpose": "結構化知識的語義表示和實體檢索",
                "technical_details": {
                    "granularity": "單個實體及其豐富描述信息",
                    "processing": "實體提取後的描述文本向量化",
                    "content": "實體名稱 + 類型 + 屬性 + 描述",
                    "model": "同 text-embedding 模型保持一致性",
                    "storage": "lancedb/entity-description/"
                },
                "use_cases": [
                    "實體相似性分析和發現",
                    "基於實體的關聯查詢",
                    "知識圖譜節點的向量表示",
                    "支援概念級別的語義檢索"
                ],
                "advantages": [
                    "✅ 結構化知識的向量表示",
                    "✅ 支援實體級別的相似性檢索",
                    "✅ 連接文本和知識圖譜的橋樑",
                    "✅ 實現語義層面的實體關聯"
                ]
            },
            
            # 🏘️ 第三層：Community Full Content Embeddings (社群內容嵌入)
            "community_level": {
                "purpose": "主題社群的整體語義和概念性檢索",
                "technical_details": {
                    "granularity": "整個社群的報告和摘要內容",
                    "processing": "社群報告全文向量化",
                    "content": "社群摘要 + 關鍵實體 + 主題分析",
                    "aggregation": "多個實體和關係的聚合表示",
                    "storage": "lancedb/community-full_content/"
                },
                "use_cases": [
                    "主題級別的概念檢索",
                    "Global Search 的核心數據源",
                    "跨社群的主題關聯分析",
                    "支援高層次的概念性問答"
                ],
                "advantages": [
                    "✅ 主題社群的整體語義表示",
                    "✅ 支援概念性和總結性查詢",
                    "✅ 發現隱藏的主題關聯模式",
                    "✅ 實現全域性的知識洞察"
                ]
            }
        }
        
        return embedding_architecture
    
    def demonstrate_retrieval_strategy_differences(self):
        """🔍 檢索策略的技術差異對比
        
        展現不同層級嵌入在檢索策略中的具體應用
        和技術實現差異
        """
        
        retrieval_strategies = {
            # 🎯 Local Search 檢索流程
            "local_search_flow": {
                "step_1_query_embedding": {
                    "action": "將用戶查詢轉換為向量表示",
                    "technical": "使用相同的 embedding 模型確保向量空間一致性"
                },
                "step_2_multi_level_matching": {
                    "text_unit_search": {
                        "method": "餘弦相似度檢索",
                        "target": "text_unit-text vectors",
                        "result": "最相關的文本片段"
                    },
                    "entity_search": {
                        "method": "實體向量空間檢索", 
                        "target": "entity-description vectors",
                        "result": "相關實體及其描述"
                    }
                },
                "step_3_relationship_expansion": {
                    "action": "基於實體關係圖擴展上下文",
                    "technical": "利用 relationships.parquet 中的關係數據"
                },
                "step_4_context_assembly": {
                    "action": "組裝檢索到的多層級上下文",
                    "composition": "文本片段 + 實體描述 + 關係信息"
                },
                "step_5_llm_generation": {
                    "action": "基於豐富上下文生成精確回答",
                    "advantage": "有具體證據支撐的高質量回答"
                }
            },
            
            # 🌐 Global Search 檢索流程
            "global_search_flow": {
                "step_1_community_matching": {
                    "action": "查詢向量與社群向量的匹配",
                    "target": "community-full_content vectors",
                    "method": "相似度排序選擇相關社群"
                },
                "step_2_map_phase": {
                    "action": "並行分析多個社群報告",
                    "processing": "每個社群獨立生成局部答案",
                    "concurrency": "多社群並行處理提升效率"
                },
                "step_3_reduce_phase": {
                    "action": "整合所有局部答案",
                    "technical": "LLM 驅動的摘要和合成",
                    "goal": "消除重複，發現關聯模式"
                },
                "step_4_synthesis": {
                    "action": "生成最終的綜合洞察",
                    "advantage": "全域性的深度分析和概念理解"
                }
            }
        }
        
        return retrieval_strategies
    
    def analyze_technical_innovations(self):
        """💡 GraphRAG 技術創新點分析
        
        相比傳統 RAG 的技術改進和創新之處
        """
        
        innovations = {
            "vs_traditional_rag": {
                "traditional_rag_limitations": [
                    "❌ 單一層級的文本塊向量化",
                    "❌ 缺乏結構化知識表示",
                    "❌ 無法捕捉實體間關係",
                    "❌ 檢索結果缺乏語義組織",
                    "❌ 難以回答概念性問題"
                ],
                
                "graphrag_innovations": [
                    "✅ 多層級向量化策略",
                    "✅ 實體-關係-社群的結構化表示",
                    "✅ 基於圖算法的智能組織",
                    "✅ 雙重檢索策略適應不同問題類型",
                    "✅ Map-Reduce 模式的全域分析能力"
                ]
            },
            
            "technical_advantages": {
                "semantic_hierarchy": {
                    "description": "多層次語義抽象",
                    "benefit": "從具體文本到抽象概念的完整覆蓋"
                },
                "graph_structure": {
                    "description": "圖結構化的知識組織",
                    "benefit": "捕捉實體間的複雜關係和依賴"
                },
                "community_detection": {
                    "description": "智能主題社群發現",
                    "benefit": "自動識別隱藏的知識主題群組"
                },
                "dual_retrieval": {
                    "description": "精確性和全面性兼顧",
                    "benefit": "Local 和 Global 策略適應不同查詢需求"
                }
            },
            
            "performance_implications": {
                "accuracy": "結構化知識表示提升回答準確性",
                "completeness": "社群分析發現傳統檢索遺漏的關聯",
                "interpretability": "可追溯的實體關係鏈增強可解釋性",
                "scalability": "分層架構支援大規模知識庫的高效檢索"
            }
        }
        
        return innovations
```

### 🎤 面試講解要點

1. **技術創新理解**：
   - "GraphRAG 的核心創新是三層級嵌入設計，實現了多粒度的語義表示"
   - "相比傳統 RAG 的單一文本塊向量化，GraphRAG 建立了結構化的知識表示"

2. **架構設計洞察**：
   - "從文本單元到實體再到社群的層次化設計，覆蓋了不同抽象層級的語義"
   - "每個層級都有明確的用途和技術實現方式"

3. **檢索策略對比**：
   - "Local Search 利用實體向量實現精確檢索，Global Search 通過社群向量實現全域分析"
   - "雙重策略的設計讓系統能適應不同類型的查詢需求"

---

## 🎤 面試展示總結

### 💎 核心技術亮點

1. **GraphRAG 深度掌握**：
   - 理解 6 階段知識圖譜構建流程
   - 掌握 Local vs Global 檢索策略差異
   - 認識多層級向量化的技術創新

2. **LangChain 運作機制**：
   - 掌握 Agent 的完整決策執行流程
   - 理解 Tool 系統的設計和選擇邏輯
   - 展現智能化的用戶體驗設計

3. **系統整合能力**：
   - 成功整合 Microsoft GraphRAG 到對話式 AI 系統
   - 實現企業級的安全性和性能優化
   - 展現端到端的產品化思維

### 🚀 面試成功關鍵

**技術理解深度** → 不只會用，更理解原理和設計思想
**實戰經驗豐富** → 有實際的開發、調試、優化經驗  
**系統整合能力** → 能將多個技術有機整合成完整產品
**問題解決能力** → 面對新技術能快速學習並解決實際問題

通過這 5 個核心程式碼片段的展示，你將能夠充分證明自己在 AI 系統開發方面的專業能力和技術深度！