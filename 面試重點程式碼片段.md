# 面試重點程式碼片段 - GraphRAG 技術原理 & LangChain 運作機制

## 🎯 面試重點說明策略

### GraphRAG 核心技術理解
1. **知識圖譜構建** - 實體提取、關係分析、社群偵測
2. **檢索增強生成** - Local Search vs Global Search 的技術差異
3. **向量化與嵌入** - 文本嵌入、實體嵌入、社群嵌入的層次化設計
4. **多層級分析** - 從文本單元到社群的階層化知識組織

### LangChain 運作機制理解
1. **Agent 決策邏輯** - Tool Selection 和 Reasoning Chain
2. **Memory 管理** - 上下文保持和對話歷史處理
3. **Tool 執行流程** - 從意圖識別到工具調用的完整流程
4. **Prompt Engineering** - 系統提示設計和工具描述優化

---

## 🧠 GraphRAG 技術原理與實現

### 1. GraphRAG 知識圖譜構建流程 ⭐⭐⭐⭐⭐

**面試核心：** 理解 GraphRAG 的多階段知識處理流程

```python
async def index_knowledge_base(self, kb_id: str) -> Dict[str, Any]:
    """🔍 GraphRAG 索引建立 - 完整的知識圖譜構建流程
    
    GraphRAG 核心處理階段：
    1. Text Chunking - 文本分塊處理
    2. Entity Extraction - 實體提取 
    3. Relationship Extraction - 關係提取
    4. Community Detection - 社群偵測
    5. Embedding Generation - 向量嵌入生成
    6. Graph Construction - 知識圖譜構建
    """
    try:
        kb_path = Path(kb_info['path'])
        
        # 🔐 GraphRAG 需要安全的 API key 傳遞
        env = os.environ.copy()
        env["GRAPHRAG_API_KEY"] = self.openai_api_key
        
        # 🚀 執行 Microsoft GraphRAG 完整流水線
        # 這個命令會觸發以下 GraphRAG 內部流程：
        """
        GraphRAG 內部處理流程：
        
        📝 Stage 1: Text Processing
        - 讀取 input/ 目錄中的所有文件
        - 將長文本分割成可處理的文本單元 (text units)
        - 每個文本單元通常 300-1200 tokens
        
        🎯 Stage 2: Entity Extraction  
        - 使用 LLM (GPT-4) 從每個文本單元提取實體
        - 識別：人物、組織、地點、概念、事件等
        - 生成實體描述和屬性
        
        🔗 Stage 3: Relationship Extraction
        - 分析實體間的語義關係
        - 提取關係類型：包含、屬於、影響、相關等
        - 建立實體關係網絡
        
        🏘️ Stage 4: Community Detection
        - 使用圖算法 (如 Leiden algorithm) 進行社群偵測
        - 將相關實體組織成主題社群
        - 建立多層級的階層結構
        
        📊 Stage 5: Community Summarization
        - 為每個社群生成總結描述
        - 使用 LLM 分析社群的核心主題
        - 創建社群報告 (Community Reports)
        
        🔢 Stage 6: Embedding Generation
        - 為實體描述生成向量嵌入
        - 為社群內容生成向量嵌入  
        - 為文本單元生成向量嵌入
        - 儲存到 LanceDB 向量資料庫
        """
        
        result = subprocess.run([
            "python", "-m", "graphrag", "index",
            "--root", str(kb_path),
            "--verbose"
        ], capture_output=True, text=True, cwd=str(kb_path), env=env)
        
        if result.returncode == 0:
            # ✅ 索引建立成功，檢查生成的資料結構
            """
            GraphRAG 輸出結構：
            
            📁 output/
            ├── 📄 entities.parquet         # 實體資料表
            ├── 📄 relationships.parquet    # 關係資料表  
            ├── 📄 communities.parquet      # 社群資料表
            ├── 📄 community_reports.parquet # 社群報告
            ├── 📄 text_units.parquet       # 文本單元
            └── 📁 lancedb/                 # 向量資料庫
                ├── entity-description/     # 實體向量
                ├── community-full_content/ # 社群向量
                └── text_unit-text/        # 文本向量
            """
            
            return {
                'success': True,
                'message': 'GraphRAG 知識圖譜建立完成',
                'entities_extracted': '自動提取實體和關係',
                'communities_detected': '智能社群偵測完成',
                'embeddings_generated': '多層級向量嵌入生成'
            }
        
    except Exception as e:
        return {'success': False, 'error': str(e)}
```

**面試說明重點：**
- **多階段處理流程**: GraphRAG 的 6 個核心處理階段
- **實體關係提取**: 如何從非結構化文本構建結構化知識
- **社群偵測算法**: 使用圖算法組織知識結構
- **向量化策略**: 三層級嵌入（實體、社群、文本）的設計理念
- **資料輸出結構**: 理解 GraphRAG 生成的各種資料表和向量庫

### 2. GraphRAG Local vs Global Search 技術原理 ⭐⭐⭐⭐⭐

**面試核心：** 理解 GraphRAG 兩種檢索策略的技術差異

```python
async def _query(self, kb_id: str, query: str, method: str) -> Dict[str, Any]:
    """🔍 GraphRAG 雙重查詢策略 - Local Search vs Global Search
    
    GraphRAG 的兩種檢索策略：
    
    🎯 Local Search (本地搜尋):
    - 基於向量相似度的精確檢索
    - 使用實體嵌入和文本單元嵌入
    - 適合回答具體、事實性問題
    - 檢索流程：Query → Embedding → Vector Search → Entity/Text Units
    
    🌐 Global Search (全域搜尋):
    - 基於社群分析的整體洞察
    - 使用社群報告和層級結構
    - 適合回答概念性、總結性問題  
    - 檢索流程：Query → Community Analysis → Map-Reduce → Synthesis
    """
    
    try:
        kb_path = Path(kb_info['path'])
        query_type = "local" if method == "local" else "global"
        
        # 🔐 安全傳遞 API key
        env = os.environ.copy()
        env["GRAPHRAG_API_KEY"] = self.openai_api_key
        
        # 🚀 執行對應的查詢策略
        result = subprocess.run([
            "python", "-m", "graphrag", "query",
            "--root", str(kb_path),
            "--method", query_type,  # 關鍵：選擇查詢策略
            "-q", query
        ], capture_output=True, text=True, cwd=str(kb_path), env=env)
        
        """
        Local Search 內部流程：
        1. 🔢 Query Embedding: 將查詢轉換為向量
        2. 🎯 Entity Matching: 在實體嵌入空間中找相似實體
        3. 📝 Text Unit Retrieval: 檢索相關的文本單元
        4. 🔗 Relationship Expansion: 擴展相關的實體關係
        5. 📊 Context Assembly: 組裝檢索到的上下文
        6. 💬 LLM Generation: 基於檢索上下文生成回答
        
        技術特點：
        - 高精度：直接匹配相關實體和文本
        - 可解釋：可以追溯到具體的文本來源
        - 適用場景：「誰是...？」「什麼是...？」「何時發生...？」
        """
        
        """
        Global Search 內部流程：
        1. 🏘️ Community Selection: 選擇相關的知識社群
        2. 🗺️ Map Phase: 對每個社群的報告進行分析
           - 並行處理多個社群報告
           - 每個社群生成局部答案
        3. 🔄 Reduce Phase: 將所有局部答案整合
           - 使用 LLM 進行摘要和合成
           - 消除重複和矛盾信息
        4. 📋 Final Synthesis: 生成最終的綜合回答
        
        技術特點：
        - 全面性：整合多個社群的知識
        - 洞察力：發現隱藏的主題和關聯
        - 適用場景：「總體趨勢是...？」「主要影響因素有...？」
        """
        
        if result.returncode == 0:
            answer = result.stdout.strip()
            
            # 📊 根據查詢類型提供技術解釋
            if method == "local":
                explanation = "基於向量相似度的精確實體檢索"
            else:
                explanation = "基於社群分析的 Map-Reduce 整合查詢"
            
            return {
                'success': True,
                'answer': answer,
                'query_type': method,
                'technical_approach': explanation,
                'kb_id': kb_id
            }
            
    except Exception as e:
        return {'success': False, 'error': str(e)}
```

**面試說明重點：**
- **Local Search 原理**: 向量相似度檢索 + 實體關係擴展
- **Global Search 原理**: Map-Reduce 模式的社群分析
- **技術選擇邏輯**: 何時使用哪種檢索策略
- **檢索性能差異**: Local 快速精確 vs Global 全面深入
- **適用場景對比**: 事實性問題 vs 概念性分析

async def index_knowledge_base(self, kb_id: str) -> Dict[str, Any]:
    """🚀 安全的索引建立 - 展現安全執行和錯誤處理"""
    try:
        # 📊 狀態管理
        self.knowledge_bases[kb_id]['status'] = 'indexing'
        self._save_knowledge_bases()
        
        # 🔐 安全的環境變數注入
        env = os.environ.copy()
        env["GRAPHRAG_API_KEY"] = self.openai_api_key or self.deepseek_api_key
        
        # 🎯 執行 GraphRAG 索引建立
        result = subprocess.run([
            "python", "-m", "graphrag", "index",
            "--root", str(kb_path),
            "--verbose"
        ], capture_output=True, text=True, cwd=str(kb_path), env=env)
        
        # ✅ 成功處理
        if result.returncode == 0:
            self.knowledge_bases[kb_id]['status'] = 'ready'
            self.knowledge_bases[kb_id]['last_indexed'] = datetime.now().isoformat()
            self._save_knowledge_bases()
            
            return {
                'success': True,
                'message': 'GraphRAG 知識圖譜建立完成',
                'kb_id': kb_id
            }
        else:
            # ❌ 錯誤處理和狀態恢復
            self.knowledge_bases[kb_id]['status'] = 'error'
            self._save_knowledge_bases()
            
            return {
                'success': False,
                'message': f'GraphRAG 索引建立失敗: {result.stderr}',
                'error': result.stderr
            }
            
    except Exception as e:
        # 🛡️ 全面的異常處理
        logging.error(f"Error indexing GraphRAG knowledge base {kb_id}: {e}")
        self.knowledge_bases[kb_id]['status'] = 'error'
        self._save_knowledge_bases()
        
        return {
            'success': False,
            'message': 'GraphRAG 索引建立失敗',
            'error': str(e)
        }
```

**面試說明重點：**
- **安全第一**: 避免 API key 洩露的設計
- **狀態管理**: 完整的狀態追踪和持久化
- **錯誤處理**: 多層次的錯誤處理和恢復機制
- **環境隔離**: 通過環境變數安全傳遞敏感信息

### 3. 智能查詢系統 ⭐⭐⭐⭐

**面試價值：** 展現複雜業務邏輯處理能力

```python
async def _query(self, kb_id: str, query: str, method: str) -> Dict[str, Any]:
    """🧠 智能查詢系統 - 展現複雜業務邏輯處理"""
    try:
        # 🔍 知識庫驗證
        if kb_id not in self.knowledge_bases:
            raise ValueError(f"Knowledge base {kb_id} not found")
        
        kb_info = self.knowledge_bases[kb_id]
        
        # 📋 狀態檢查 - 業務邏輯驗證
        if kb_info['status'] != 'ready':
            return {
                'success': False,
                'message': 'GraphRAG 知識庫尚未準備就緒，請先建立索引'
            }
        
        kb_path = Path(kb_info['path'])
        
        # 🎯 查詢類型選擇 - 展現業務邏輯設計
        query_type = "local" if method == "local" else "global"
        
        # 🔐 安全的查詢執行
        env = os.environ.copy()
        env["GRAPHRAG_API_KEY"] = self.openai_api_key or self.deepseek_api_key
        
        result = subprocess.run([
            "python", "-m", "graphrag", "query",
            "--root", str(kb_path),
            "--method", query_type,
            "-q", query  # 🐛 修復後的正確 CLI 語法
        ], capture_output=True, text=True, cwd=str(kb_path), env=env)
        
        # 📊 結果處理和格式化
        if result.returncode == 0:
            answer = result.stdout.strip()
            
            return {
                'success': True,
                'answer': answer,
                'query_type': method,
                'kb_id': kb_id
            }
        else:
            logging.error(f"GraphRAG query failed: {result.stderr}")
            return {
                'success': False,
                'message': f'查詢失敗: {result.stderr}',
                'error': result.stderr
            }
            
    except Exception as e:
        logging.error(f"Error querying GraphRAG knowledge base {kb_id}: {e}")
        return {
            'success': False,
            'message': '查詢過程中發生錯誤',
            'error': str(e)
        }
```

**面試說明重點：**
- **業務邏輯驗證**: 完整的前置條件檢查
- **錯誤分類**: 不同類型錯誤的分類處理
- **CLI 整合**: 展現與外部工具的整合能力
- **結果標準化**: 統一的回應格式設計

---

### 3. GraphRAG 向量化與嵌入策略 ⭐⭐⭐⭐

**面試核心：** 理解 GraphRAG 的多層級向量化設計

```python
def understand_graphrag_embedding_strategy():
    """🔢 GraphRAG 向量化策略 - 三層級嵌入設計
    
    GraphRAG 的創新之處在於多層級的向量化策略：
    
    📊 第一層：Text Unit Embeddings (文本單元嵌入)
    - 將原始文本分塊後的每個文本單元向量化
    - 保留原始語義信息
    - 用於 Local Search 的精確檢索
    
    🎯 第二層：Entity Description Embeddings (實體描述嵌入)  
    - 將提取的實體及其描述向量化
    - 捕捉實體的語義特徵
    - 支援實體級別的相似性檢索
    
    🏘️ 第三層：Community Full Content Embeddings (社群內容嵌入)
    - 將整個社群的報告內容向量化
    - 包含社群內所有實體和關係的總結
    - 用於 Global Search 的主題級檢索
    """
    
    embedding_structure = {
        "text_units": {
            "purpose": "原始文本的語義保留",
            "granularity": "文本塊級別 (300-1200 tokens)",
            "use_case": "精確的文本檢索和引用",
            "storage": "lancedb/text_unit-text/"
        },
        
        "entities": {
            "purpose": "結構化知識的語義表示", 
            "granularity": "單個實體及其屬性",
            "use_case": "實體相似性和關係發現",
            "storage": "lancedb/entity-description/"
        },
        
        "communities": {
            "purpose": "主題社群的整體語義",
            "granularity": "社群級別的概念聚合", 
            "use_case": "主題分析和概念性查詢",
            "storage": "lancedb/community-full_content/"
        }
    }
    
    """
    向量檢索策略對比：
    
    🎯 Local Search 策略：
    1. Query → Text Unit Vectors (直接文本匹配)
    2. Query → Entity Vectors (實體相似性)
    3. 結合檢索結果 + 實體關係圖
    4. 生成基於具體證據的回答
    
    🌐 Global Search 策略：
    1. Query → Community Vectors (主題匹配)
    2. 選擇相關社群集合
    3. 並行分析多個社群報告
    4. Map-Reduce 整合生成概念性洞察
    
    技術優勢：
    - 多粒度檢索：從具體文本到抽象概念
    - 語義層次：保留不同抽象級別的語義信息  
    - 靈活查詢：根據問題類型選擇最適檢索策略
    """
    
    return embedding_structure
```

**面試說明重點：**
- **多層級設計理念**: 為什麼需要三層級向量化
- **語義抽象層次**: 從具體文本到抽象概念的語義保留
- **檢索策略差異**: 不同層級向量在查詢中的作用
- **存儲結構設計**: LanceDB 中的向量組織方式
- **技術創新點**: 相比傳統 RAG 的向量化改進

---

## 🤖 LangChain 代理運作機制

### 4. LangChain Agent 決策與執行流程 ⭐⭐⭐⭐⭐

**面試核心：** 理解 LangChain Agent 的核心運作邏輯

```python
class LangChainAgentExecution:
    """🤖 LangChain Agent 執行邏輯 - 理解 Agent 決策流程"""
    
    def __init__(self):
        # 🧠 LLM：Agent 的大腦
        self.llm = ChatDeepSeek(model="deepseek-chat", temperature=0.7)
        
        # 🛠️ Tools：Agent 可用的工具集
        self.tools = self._load_all_tools()
        
        # 💾 Memory：對話上下文管理
        self.memory = ConversationBufferWindowMemory(
            memory_key="chat_history",
            return_messages=True,
            k=10  # 保留最近 10 輪對話
        )
        
        # 🎯 Agent：決策執行器
        self.agent = create_openai_tools_agent(
            llm=self.llm,
            tools=self.tools,
            prompt=self._create_agent_prompt()
        )
        
        # 🔄 Executor：Agent 執行環境
        self.executor = AgentExecutor(
            agent=self.agent,
            tools=self.tools,
            memory=self.memory,
            verbose=True,  # 顯示決策過程
            handle_parsing_errors=True
        )
    
    async def execute_user_query(self, user_input: str, user_id: str) -> str:
        """🚀 Agent 執行流程 - 完整的決策鏈
        
        LangChain Agent 的核心執行流程：
        
        1. 📝 Input Processing：處理用戶輸入
        2. 🧠 Intent Analysis：意圖分析和理解
        3. 🔍 Tool Selection：選擇合適的工具
        4. ⚡ Tool Execution：執行工具並獲取結果
        5. 🤔 Result Analysis：分析工具執行結果
        6. 🔄 Chain of Thought：可能的多步推理
        7. 📊 Response Generation：生成最終回應
        """
        
        try:
            # 🎯 Step 1: 準備執行上下文
            execution_context = {
                "input": user_input,
                "user_id": user_id,
                "chat_history": self.memory.chat_memory.messages
            }
            
            # 🧠 Step 2: Agent 決策執行
            """
            LangChain Agent 內部決策流程：
            
            A. 🔍 Intent Recognition（意圖識別）:
               - Agent 分析用戶輸入
               - 理解用戶的真實意圖
               - 考慮對話歷史上下文
            
            B. 🛠️ Tool Selection（工具選擇）:
               - 根據工具描述匹配最佳工具
               - 考慮工具的適用場景
               - 可能選擇多個工具進行組合
            
            C. 📋 Parameter Extraction（參數提取）:
               - 從用戶輸入提取工具所需參數
               - 處理參數格式和類型轉換
               - 驗證參數的完整性
            
            D. ⚡ Tool Execution（工具執行）:
               - 調用選中的工具
               - 處理工具執行結果
               - 錯誤處理和重試邏輯
            
            E. 🤔 Result Reasoning（結果推理）:
               - 分析工具返回的結果
               - 判斷是否需要調用更多工具
               - 進行多步推理鏈
            
            F. 📝 Response Synthesis（回應合成）:
               - 將工具結果整合成自然語言
               - 保持一致的語調和風格
               - 添加相關的解釋和建議
            """
            
            result = await self.executor.ainvoke(execution_context)
            
            # 📊 Step 3: 結果處理和記憶更新
            response = result.get("output", "抱歉，我無法理解您的請求。")
            
            # 💾 更新對話記憶
            self.memory.save_context(
                {"input": user_input},
                {"output": response}
            )
            
            return response
            
        except Exception as e:
            logging.error(f"Agent execution error: {e}")
            return "抱歉，處理您的請求時發生了錯誤。請稍後再試。"
    
    def _create_agent_prompt(self) -> ChatPromptTemplate:
        """🎯 Agent 提示設計 - 核心的 Prompt Engineering
        
        LangChain Agent 的提示結構：
        1. System Message：定義 Agent 的角色和行為準則
        2. Tool Descriptions：自動生成的工具描述
        3. Memory Placeholder：對話歷史的插入點
        4. Human Message：用戶輸入的處理
        5. Agent Scratchpad：Agent 的思考過程記錄
        """
        
        system_prompt = """你是 HealMate，一個溫暖、智慧的 AI 助理。

🧠 **核心決策邏輯：**
1. 🔍 仔細分析用戶的真實需求和意圖
2. 🛠️ 優先使用工具獲取準確信息，而不是憑空猜測
3. 💭 如果需要更多上下文，先查詢用戶的歷史記錄
4. 🎯 選擇最適合的工具組合來回答問題

🛠️ **工具使用策略：**
- 📚 知識圖譜相關：使用 Microsoft GraphRAG 工具
- 💝 情感支持：使用情感分析和策略建議工具  
- 🔮 尋求指引：使用塔羅占卜工具
- 🌟 運勢查詢：使用星座運勢工具
- 📊 個人化：先使用記憶工具了解用戶狀態

🗣️ **回應原則：**
- 自然地融合工具結果，避免生硬的引用
- 保持溫暖、理解和支持的語調
- 提供actionable的建議和後續步驟
- 展現同理心和個人化關懷

請根據用戶的輸入，選擇最合適的工具並提供有溫度的回應。"""

        return ChatPromptTemplate.from_messages([
            ("system", system_prompt),
            MessagesPlaceholder("chat_history"),
            ("human", "{input}"),
            MessagesPlaceholder("agent_scratchpad")
        ])
```

**面試說明重點：**
- **Agent 架構理解**: LLM + Tools + Memory + Executor 的組合設計
- **決策流程分析**: 從意圖識別到工具執行的完整鏈路
- **Prompt Engineering**: 系統提示如何影響 Agent 行為
- **記憶管理機制**: 對話上下文的保持和更新策略
- **錯誤處理設計**: 優雅的錯誤恢復和用戶引導

### 5. LangChain Tool 設計與選擇邏輯 ⭐⭐⭐⭐

**面試核心：** 理解 LangChain Tool 的設計模式和選擇機制

```python
class LangChainToolDesign:
    """🛠️ LangChain Tool 設計原理 - 理解工具系統架構"""
    
    def create_intelligent_tool(self):
        """🎯 智能工具設計 - Tool 的核心要素
        
        LangChain Tool 的核心組成：
        1. Name: 工具的唯一標識符
        2. Description: Agent 選擇工具的關鍵依據  
        3. Function/Coroutine: 實際執行的業務邏輯
        4. Args Schema: 參數驗證和類型檢查
        """
        
        # 📚 Microsoft GraphRAG 查詢工具設計
        query_tool = Tool(
            name="QueryMicrosoftGraphRAG",
            description="""
            🧠 GraphRAG 智能查詢工具
            
            **使用時機判斷邏輯：**
            - 用戶詢問基於已建立知識庫的問題
            - 需要從文件內容中提取答案
            - 問題涉及實體關係或概念性分析
            - 用戶明確要求使用 GraphRAG 技術
            
            **技術能力：**
            - 支援本地和全域兩種查詢模式
            - 基於實體關係圖的推理分析
            - 社群偵測驅動的主題洞察
            
            **輸入格式：** 用戶的自然語言問題
            **輸出格式：** 基於知識圖譜的結構化回答
            """,
            coroutine=self._execute_graphrag_query,
            # args_schema=QueryToolArgs  # 可選：參數驗證
        )
        
        return query_tool
    
    async def _execute_graphrag_query(self, query: str, user_id: str = "default_user"):
        """🚀 工具執行邏輯 - 展現工具內部運作"""
        
        # 🔍 Step 1: 狀態驗證邏輯
        """
        工具執行的前置檢查：
        - 驗證用戶權限和上下文
        - 檢查必要資源的可用性
        - 進行參數有效性驗證
        """
        knowledge_bases = microsoft_graphrag_service.get_knowledge_bases(user_id)
        
        if not knowledge_bases:
            # 🚫 引導式錯誤處理
            return self._generate_guidance_response(
                issue="no_knowledge_base",
                next_steps=[
                    "建立新的知識圖譜",
                    "上傳相關文件",
                    "等待索引建立完成"
                ]
            )
        
        # 📋 Step 2: 智能資源選擇
        """
        資源選擇策略：
        - 優先選擇狀態為 'ready' 的知識庫
        - 選擇最近更新或最相關的知識庫
        - 考慮知識庫的內容主題匹配度
        """
        ready_kbs = [kb for kb in knowledge_bases if kb['status'] == 'ready']
        
        if not ready_kbs:
            return self._generate_guidance_response(
                issue="kb_not_indexed",
                current_status=self._analyze_kb_status(knowledge_bases)
            )
        
        # 🎯 Step 3: 查詢策略選擇
        """
        GraphRAG 查詢策略決策：
        
        Local Search 適用場景：
        - 具體的事實性問題
        - 需要精確引用來源
        - 問題範圍明確且具體
        
        Global Search 適用場景：
        - 概念性或總結性問題
        - 需要跨主題整合分析
        - 探索性或開放性問題
        """
        query_strategy = self._determine_query_strategy(query)
        selected_kb = self._select_best_kb(ready_kbs, query)
        
        # ⚡ Step 4: 執行查詢並處理結果
        if query_strategy == "global":
            result = await microsoft_graphrag_service.query_global(
                selected_kb['kb_id'], query
            )
        else:
            result = await microsoft_graphrag_service.query_local(
                selected_kb['kb_id'], query
            )
        
        # 📊 Step 5: 結果格式化和增強
        return self._format_graphrag_response(result, selected_kb, query_strategy)
    
    def _determine_query_strategy(self, query: str) -> str:
        """🧠 查詢策略決策算法 - AI 工具的智能邏輯"""
        
        # 🎯 Local Search 關鍵詞模式
        local_indicators = [
            "什麼是", "誰是", "何時", "在哪裡",
            "定義", "解釋", "具體", "詳細"
        ]
        
        # 🌐 Global Search 關鍵詞模式  
        global_indicators = [
            "總結", "整體", "趨勢", "關係", "影響",
            "比較", "分析", "概述", "主要"
        ]
        
        # 🔍 模式匹配邏輯
        local_score = sum(1 for indicator in local_indicators if indicator in query)
        global_score = sum(1 for indicator in global_indicators if indicator in query)
        
        # 📊 決策邏輯
        if global_score > local_score:
            return "global"
        elif local_score > 0:
            return "local"
        else:
            # 預設策略：對於模糊問題使用全域搜尋
            return "global"
    
    def demonstrate_tool_selection_mechanism(self):
        """🤖 Agent 工具選擇機制 - LangChain 的核心邏輯
        
        LangChain Agent 如何選擇工具：
        
        1. 🔍 Description Matching（描述匹配）:
           - Agent 將用戶輸入與工具描述進行語義匹配
           - 使用 LLM 的理解能力判斷最佳匹配
           - 考慮工具的適用場景和限制
        
        2. 📊 Context Awareness（上下文感知）:
           - 考慮對話歷史和用戶狀態
           - 分析當前對話的主題和意圖
           - 整合多輪對話的信息脈絡
        
        3. 🎯 Multi-Tool Reasoning（多工具推理）:
           - 可能選擇多個工具進行組合使用
           - 根據前一個工具的結果決定下一步
           - 實現複雜的多步推理鏈
        
        4. 🔄 Feedback Loop（反饋循環）:
           - 根據工具執行結果調整策略
           - 可能重新選擇或調用其他工具
           - 持續優化直到獲得滿意結果
        """
        
        tool_selection_flow = {
            "step_1_intent_analysis": "分析用戶意圖和上下文",
            "step_2_tool_matching": "基於描述匹配最相關工具",
            "step_3_parameter_extraction": "提取工具所需參數",
            "step_4_execution": "執行選中的工具",
            "step_5_result_evaluation": "評估結果並決定下一步",
            "step_6_response_generation": "生成最終用戶回應"
        }
        
        return tool_selection_flow
```

**面試說明重點：**
- **Tool 設計原則**: Name、Description、Function 的設計考量
- **智能選擇邏輯**: Agent 如何根據描述選擇工具
- **查詢策略決策**: Local vs Global 的智能選擇算法
- **錯誤處理機制**: 優雅的引導式錯誤處理
- **多步推理能力**: Tool 組合使用的複雜邏輯

### 6. 智能代理初始化與配置 ⭐⭐⭐⭐

**面試價值：** 展現系統初始化和配置管理能力

```python
class HealMateAgent:
    """🤖 HealMate 智能代理 - 展現完整的代理系統設計"""
    
    def __init__(self):
        # 🧠 LLM 提供商選擇邏輯
        self.llm = self._initialize_llm()
        
        # 🛠️ 工具生態系統載入
        self.tools = self._load_comprehensive_tools()
        
        # 💾 記憶管理系統
        self.memory = ConversationBufferWindowMemory(
            memory_key="chat_history",
            return_messages=True,
            k=10  # 保留最近 10 輪對話
        )
        
        # 🎯 代理執行器建立
        self.agent = self._create_intelligent_agent()
    
    def _initialize_llm(self) -> BaseChatModel:
        """🔧 智能 LLM 初始化 - 展現多提供商支援能力"""
        if not os.getenv("DEEPSEEK_API_KEY"):
            logging.error("DeepSeek API Key not provided")
            raise ValueError("DEEPSEEK_API_KEY is required")
            
        logging.info("Using DeepSeek LLM API v3")
        return ChatDeepSeek(
            api_key=os.getenv("DEEPSEEK_API_KEY"),
            model="deepseek-chat",
            temperature=0.7,
            streaming=True  # 🚀 流式回應支援
        )
    
    def _load_comprehensive_tools(self):
        """🛠️ 綜合工具載入 - 展現工具生態整合"""
        return [
            # 🧠 GraphRAG 工具組
            *MICROSOFT_GRAPHRAG_TOOLS,
            
            # 💝 心理健康工具組  
            strategy_tool,
            emotion_analysis_tool,
            
            # 🔮 娛樂與占卜工具組
            tarot_reading_tool,
            random_tarot_reading_tool,
            horoscope_tool,
            
            # 📊 記憶與個人化工具組
            knowledge_base_tool,
            # mood_history_tool 會動態建立
        ]

# 🎯 代理系統提示設計 - 展現提示工程專業能力
AGENT_SYSTEM_PROMPT = """你是一個名為「HealMate」的AI助理，充滿同理心、智慧和溫暖。

🎯 **工具使用優先級策略：**
1. **GraphRAG 工具** - 當用戶提到「知識圖譜」、「建立知識庫」、或需要深度分析時
2. **情感分析工具** - 當用戶表達情緒困擾或需要心理支持時  
3. **塔羅占卜工具** - 當用戶尋求人生指引或面臨重要決定時
4. **記憶工具** - 在提供個人化回應前先查詢用戶的歷史互動

🌟 **行為準則：**
- 🎯 優先使用工具，基於實際數據而非臆測回應
- 🤝 善用用戶的心情歷史，提供個人化和有溫度的回應  
- 💬 自然地將工具輸出融入對話，避免生硬的工具引用
- ❤️ 永遠保持溫暖、理解和支持的語氣

🧠 **智能決策邏輯：**
當用戶提出複雜需求時，可以組合多個工具來提供完整解答。
例如：先查詢心情歷史了解背景，再使用相應的專業工具提供建議。"""
```

**面試說明重點：**
- **架構設計**: 清晰的元件分離和組織
- **配置管理**: 靈活的初始化和配置策略
- **提示工程**: 專業的系統提示設計
- **擴展性**: 易於擴展的工具整合架構

---

## 🔧 進階技術實現

### 7. 非同步任務管理 ⭐⭐⭐⭐

**面試價值：** 展現並發處理和任務管理能力

```python
from fastapi import BackgroundTasks
import asyncio
from concurrent.futures import ThreadPoolExecutor

class GraphRAGTaskManager:
    """📋 GraphRAG 任務管理器 - 展現並發處理能力"""
    
    def __init__(self, max_workers: int = 4):
        self.executor = ThreadPoolExecutor(max_workers=max_workers)
        self.indexing_tasks = {}  # 追踪正在進行的任務
        self.task_results = {}    # 快取任務結果
    
    async def start_background_indexing(self, kb_id: str) -> Dict[str, Any]:
        """🚀 啟動背景索引任務 - 展現非同步處理設計"""
        
        # 🔍 檢查是否已有任務在執行
        if kb_id in self.indexing_tasks:
            task = self.indexing_tasks[kb_id]
            if not task.done():
                return {
                    "success": True,
                    "status": "already_running",
                    "message": "索引建立已在進行中"
                }
        
        # 🎯 建立新的背景任務
        task = asyncio.create_task(
            self._run_indexing_with_monitoring(kb_id)
        )
        self.indexing_tasks[kb_id] = task
        
        return {
            "success": True, 
            "status": "started",
            "task_id": id(task),
            "message": "GraphRAG 索引建立已開始"
        }
    
    async def _run_indexing_with_monitoring(self, kb_id: str):
        """📊 帶監控的索引執行 - 展現任務監控能力"""
        try:
            start_time = time.time()
            
            # 🔄 更新任務狀態
            await self._update_task_status(kb_id, "processing")
            
            # 🏃‍♂️ 在執行器中運行索引建立（避免阻塞）
            loop = asyncio.get_event_loop()
            result = await loop.run_in_executor(
                self.executor,
                self._blocking_index_operation,
                kb_id
            )
            
            # ⏱️ 計算執行時間
            execution_time = time.time() - start_time
            
            # 📊 記錄結果
            self.task_results[kb_id] = {
                **result,
                "execution_time": execution_time,
                "completed_at": datetime.now().isoformat()
            }
            
            # ✅ 更新最終狀態
            final_status = "completed" if result["success"] else "failed"
            await self._update_task_status(kb_id, final_status)
            
            return result
            
        except Exception as e:
            # ❌ 錯誤處理和記錄
            logging.error(f"Background indexing failed for {kb_id}: {e}")
            
            self.task_results[kb_id] = {
                "success": False,
                "error": str(e),
                "failed_at": datetime.now().isoformat()
            }
            
            await self._update_task_status(kb_id, "failed")
            raise
            
        finally:
            # 🧹 清理任務記錄
            self.indexing_tasks.pop(kb_id, None)

# 🌐 FastAPI 背景任務整合
@router.post("/microsoft/knowledge-base/{kb_id}/index")
async def index_microsoft_knowledge_base(
    kb_id: str, 
    background_tasks: BackgroundTasks
) -> Dict[str, Any]:
    """⚡ FastAPI 背景任務端點 - 展現 Web 框架整合"""
    try:
        # 🚀 啟動背景任務
        background_tasks.add_task(
            microsoft_graphrag_service.index_knowledge_base, 
            kb_id
        )
        
        return {
            "success": True,
            "message": "Microsoft GraphRAG 索引建立已開始，這可能需要幾分鐘時間",
            "type": "microsoft_graphrag",
            "estimated_time": "2-5 分鐘"
        }
        
    except Exception as e:
        logging.error(f"Error starting indexing: {e}")
        raise HTTPException(
            status_code=500, 
            detail=f"啟動索引建立失敗: {str(e)}"
        )
```

**面試說明重點：**
- **並發設計**: ThreadPoolExecutor 與 asyncio 的結合使用
- **任務監控**: 完整的任務狀態追踪和結果記錄
- **資源管理**: 任務記憶體的清理和資源釋放
- **Web 整合**: 與 FastAPI 背景任務的無縫整合

### 8. 智能快取系統 ⭐⭐⭐⭐

**面試價值：** 展現性能優化和快取設計能力

```python
import redis
import pickle
import hashlib
from functools import wraps
from typing import Optional, Any, Callable

class GraphRAGIntelligentCache:
    """🚀 GraphRAG 智能快取系統 - 展現性能優化能力"""
    
    def __init__(self, redis_host: str = "redis", redis_port: int = 6379):
        self.redis_client = redis.Redis(
            host=redis_host, 
            port=redis_port, 
            db=0,
            decode_responses=False  # 支援二進制資料
        )
        
        # 🕐 不同類型資料的快取時間策略
        self.cache_ttl = {
            "query_result": 3600,      # 查詢結果 1 小時
            "knowledge_base": 7200,    # 知識庫資訊 2 小時  
            "embedding": 86400,        # 嵌入向量 1 天
            "graph_structure": 43200   # 圖結構 12 小時
        }
    
    def cached_query(self, cache_type: str = "query_result"):
        """🎯 智能查詢快取裝飾器 - 展現裝飾器設計模式"""
        def decorator(func: Callable) -> Callable:
            @wraps(func)
            async def wrapper(*args, **kwargs) -> Any:
                # 🔑 生成智能快取鍵值
                cache_key = self._generate_intelligent_cache_key(
                    func.__name__, cache_type, *args, **kwargs
                )
                
                # 🔍 嘗試從快取取得結果
                cached_result = await self._get_from_cache(cache_key)
                if cached_result is not None:
                    logging.info(f"Cache hit for {func.__name__}: {cache_key}")
                    return cached_result
                
                # 🔄 執行原始函數
                logging.info(f"Cache miss for {func.__name__}: {cache_key}")
                result = await func(*args, **kwargs)
                
                # 💾 儲存結果到快取
                await self._set_to_cache(
                    cache_key, 
                    result, 
                    self.cache_ttl[cache_type]
                )
                
                return result
            return wrapper
        return decorator
    
    def _generate_intelligent_cache_key(self, func_name: str, cache_type: str, 
                                      *args, **kwargs) -> str:
        """🧠 智能快取鍵生成 - 展現雜湊和序列化能力"""
        
        # 📝 建立鍵值元件
        key_components = [func_name, cache_type]
        
        # 🔧 處理參數
        for arg in args:
            if isinstance(arg, str):
                key_components.append(arg)
            else:
                # 🔨 複雜物件的雜湊處理
                key_components.append(str(hash(str(arg))))
        
        # 📋 處理關鍵字參數
        for key, value in sorted(kwargs.items()):
            key_components.append(f"{key}:{value}")
        
        # 🎯 生成最終快取鍵
        cache_string = "|".join(key_components)
        cache_hash = hashlib.md5(cache_string.encode()).hexdigest()
        
        return f"graphrag:cache:{cache_hash[:16]}"
    
    async def _get_from_cache(self, cache_key: str) -> Optional[Any]:
        """📥 從快取取得資料 - 展現錯誤處理"""
        try:
            cached_data = self.redis_client.get(cache_key)
            if cached_data:
                return pickle.loads(cached_data)
            return None
        except (redis.RedisError, pickle.PickleError) as e:
            logging.warning(f"Cache get error for {cache_key}: {e}")
            return None
    
    async def _set_to_cache(self, cache_key: str, data: Any, ttl: int):
        """📤 設定快取資料 - 展現序列化處理"""
        try:
            serialized_data = pickle.dumps(data)
            self.redis_client.setex(cache_key, ttl, serialized_data)
            logging.debug(f"Cached data for {cache_key} with TTL {ttl}")
        except (redis.RedisError, pickle.PickleError) as e:
            logging.warning(f"Cache set error for {cache_key}: {e}")

# 🎯 快取應用範例
class CachedGraphRAGService(MicrosoftGraphRAGService):
    """📊 快取增強的 GraphRAG 服務 - 展現繼承和混合模式"""
    
    def __init__(self):
        super().__init__()
        self.cache = GraphRAGIntelligentCache()
    
    @GraphRAGIntelligentCache().cached_query("query_result")
    async def cached_query_global(self, kb_id: str, query: str) -> Dict[str, Any]:
        """🚀 快取版本的全域查詢"""
        return await super().query_global(kb_id, query)
    
    @GraphRAGIntelligentCache().cached_query("knowledge_base")
    async def cached_get_kb_info(self, kb_id: str) -> Optional[Dict[str, Any]]:
        """📋 快取版本的知識庫資訊查詢"""
        return super().get_knowledge_base_info(kb_id)
```

**面試說明重點：**
- **裝飾器模式**: 優雅的快取邏輯封裝
- **智能鍵生成**: 考慮參數的智能快取鍵策略
- **TTL 策略**: 不同資料類型的差異化快取時間
- **錯誤處理**: Redis 和序列化的完整錯誤處理

---

---

## 💡 面試展示策略

### 🎯 重點程式碼片段優先級

#### ⭐⭐⭐⭐⭐ 必展示 (核心技術理解)
1. **GraphRAG 知識圖譜構建流程** - 展現對 GraphRAG 技術原理的深度理解
2. **Local vs Global Search 技術原理** - 展現對檢索策略的技術掌握
3. **LangChain Agent 決策執行流程** - 展現對 Agent 運作機制的理解

#### ⭐⭐⭐⭐ 重要展示 (技術應用能力)
4. **GraphRAG 向量化策略** - 展現對多層級嵌入設計的理解
5. **LangChain Tool 設計邏輯** - 展現對工具系統的深度掌握

### 📊 面試時間分配建議 (總計 20 分鐘)

#### Phase 1: GraphRAG 技術原理 (10 分鐘)
1. **開場技術深度** (4 分鐘): GraphRAG 知識圖譜構建流程
   - 重點：6 階段處理流程的技術細節
   - 說明：實體提取 → 關係分析 → 社群偵測 → 向量化

2. **檢索策略對比** (4 分鐘): Local vs Global Search 原理
   - 重點：兩種策略的技術差異和適用場景
   - 說明：向量檢索 vs Map-Reduce 社群分析

3. **向量化創新** (2 分鐘): 多層級嵌入設計
   - 重點：三層級向量化的技術創新點
   - 說明：相比傳統 RAG 的改進之處

#### Phase 2: LangChain 運作機制 (8 分鐘)
4. **Agent 架構解析** (5 分鐘): LangChain Agent 決策流程
   - 重點：LLM + Tools + Memory + Executor 的協作機制
   - 說明：從意圖識別到工具執行的完整決策鏈

5. **工具系統設計** (3 分鐘): Tool 選擇和執行邏輯
   - 重點：工具描述設計和智能選擇算法
   - 說明：多工具推理和錯誤處理機制

#### Phase 3: 技術整合總結 (2 分鐘)
6. **系統整合優勢**: GraphRAG + LangChain 的協同效果
   - 展現端到端的技術整合能力
   - 說明產品化思維和用戶體驗考量

### 🎤 技術說明重點

#### GraphRAG 技術說明要點：
- **多階段處理**: 詳細解釋每個階段的技術原理
- **檢索策略**: 強調 Local 精確性 vs Global 全面性的技術差異
- **向量化創新**: 說明三層級嵌入相比傳統方法的優勢
- **圖算法應用**: 提及 Leiden 算法在社群偵測中的作用

#### LangChain 技術說明要點：
- **Agent 決策機制**: 解釋 LLM 如何進行工具選擇和推理
- **Tool 設計模式**: 強調 Description 在工具選擇中的關鍵作用
- **Memory 管理**: 說明對話上下文的保持和更新策略
- **錯誤處理**: 展現優雅的錯誤恢復和用戶引導機制

### 🔥 加分展示點

1. **技術深度**: 
   - 能解釋 GraphRAG 相比傳統 RAG 的技術創新
   - 理解 LangChain Agent 的內部運作邏輯

2. **實戰經驗**:
   - 展示實際遇到並解決的技術挑戰
   - 說明 CLI 整合和配置管理的實踐經驗

3. **系統思維**:
   - 展現從技術選型到產品實現的完整思考
   - 體現對用戶體驗和系統架構的綜合考量

4. **持續學習**:
   - 展示快速掌握前沿技術的學習能力
   - 說明技術調研和問題解決的方法論

### 🎯 面試成功關鍵

**技術理解深度**：不只會用，更要理解原理和設計思想
**實踐經驗豐富**：有實際的開發、調試、優化經驗
**系統整合能力**：能將多個技術有機整合成完整產品
**問題解決能力**：面對新技術能快速學習並解決實際問題